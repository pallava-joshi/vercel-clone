# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:9000
NEXT_PUBLIC_SOCKET_URL=http://localhost:9002

# Note: These are used by the frontend to connect to the api-server
# The NEXT_PUBLIC_ prefix makes them available in the browser
# For production, replace localhost with your actual API server domain
